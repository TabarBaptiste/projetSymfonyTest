{% extends 'base.html.twig' %}

{% block body %}
    <h1>Supprimer l'article "{{ article.nom }}"</h1>

    {{ form_start(deleteForm) }}
        {{ form_widget(deleteForm) }}
        <button type="submit">Confirmer la suppression</button>
    {{ form_end(deleteForm) }}
{% endblock %}
